// import {} from '../actions/index';

export const reducer = (state = {}, action) => {
    switch (action.type) {
        // case :
        //     return state
        default:
            return state
    }
}